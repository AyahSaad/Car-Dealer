<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AdminMainPage"
    android:id="@+id/drawer_layout">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/carImageView"
            android:layout_width="wrap_content"
            android:layout_height="350dp"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/admin" />

        <TextView
            android:id="@+id/DescriptionTextView"
            android:layout_width="380dp"
            android:layout_height="262dp"
            android:layout_marginLeft="20dp"
            android:layout_marginTop="0dp"
            android:layout_marginRight="40dp"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="Welcome to the Admin Dashboard -Here, you have access to powerful tools to manage and optimize car shop. Effortlessly view and manage customer profiles and interactions including the ability to delete customer accounts.  Enhance your sales strategy by adding special offers to cars in your inventory using the Offers feature."
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>


    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:headerLayout="@layout/header"
        app:menu="@menu/main_menu"
        android:id="@+id/nav_view"
        tools:ignore="MissingConstraints"
        android:layout_gravity="start"/>

</androidx.drawerlayout.widget.DrawerLayout>