<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".admin.AdminMainPage"
    android:id="@+id/drawer_layout">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/carImageView"
            android:layout_width="wrap_content"
            android:layout_height="350dp"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/cardealer" />

        <TextView
            android:id="@+id/DescriptionTextView"
            android:layout_width="380dp"
            android:layout_height="215dp"
            android:layout_marginLeft="20dp"
            android:layout_marginTop="0dp"
            android:layout_marginRight="40dp"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="This app is for our car dealing company,
            which has over 20 years of experience and offers high-quality services. We hope that you will have a good experience with it."
            android:textSize="19sp"
            android:textStyle="bold" />

    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:headerLayout="@layout/customerheader"
        app:menu="@menu/customer_menu"
        android:id="@+id/nav_view"
        tools:ignore="MissingConstraints"
        android:layout_gravity="start"
        />


</androidx.drawerlayout.widget.DrawerLayout>
